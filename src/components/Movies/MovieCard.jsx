import Movie from "./Movie";
import styled from "styled-components"; //stiluska
import { MovieData } from "./MovieData";
const imag = require.context("../../images");

const MovieCard = () => {
  return (
    <div>
      <div>more infó movies</div>
    </div>
  );
};

export default MovieCard;
